# Orderer Docker Compose Template
# Generated by nanayam node init --type orderer
version: "3.7"

volumes:
  {{.OrdererID}}:

networks:
  nanayam:
    name: nanayam

services:
  {{.OrdererID}}:
    container_name: {{.OrdererID}}
    image: hyperledger/fabric-orderer:2.5.9
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - ORDERER_GENERAL_LISTENADDRESS=0.0.0.0
      - ORDERER_GENERAL_LISTENPORT={{.OrdererPort}}
      - ORDERER_GENERAL_GENESISMETHOD=file
      - ORDERER_GENERAL_GENESISFILE=/var/hyperledger/orderer/orderer.genesis.block
      - ORDERER_GENERAL_LOCALMSPID={{.MSPID}}
      - ORDERER_GENERAL_LOCALMSPDIR=/var/hyperledger/orderer/msp
      - ORDERER_GENERAL_TLS_ENABLED=true
      - ORDERER_GENERAL_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_GENERAL_CLUSTER_CLIENTCERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_CLUSTER_CLIENTPRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_CLUSTER_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
    working_dir: /opt/gopath/src/github.com/hyperledger/fabric
    command: orderer
    volumes:
      - {{.GenesisBlock}}:/var/hyperledger/orderer/orderer.genesis.block
      - {{.CryptoPath}}/orderers/{{.OrdererID}}/msp:/var/hyperledger/orderer/msp
      - {{.CryptoPath}}/orderers/{{.OrdererID}}/tls:/var/hyperledger/orderer/tls
      - {{.OrdererID}}:/var/hyperledger/production/orderer
    ports:
      - {{.OrdererPort}}:{{.OrdererPort}}
    networks:
      - nanayam
